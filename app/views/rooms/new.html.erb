<h2>Time to make a room!</h2>

<%= form_for(:rooms, url: {action: :create}) do |f| %>
  <%= label_tag(:name, "Room name:") %>
  <%= text_field_tag(:name) %>
  <%= submit_tag("Create Room") %>
<% end %>